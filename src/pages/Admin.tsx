export default function Admin(){
    return(
        <div>
            aqui seria como un panel del admin(host de la organizacion),
            acceso a todos lo registros de cada tabla de la organizacion
            
        </div>
    )
}